/**
 * Checks if the given element is an HTML <label> element.
 *
 * @param {*} el - The element to check
 * @returns {boolean} True if the element is a <label>, false otherwise
 */
export const isLabelElement = (el) => {
  return el instanceof HTMLLabelElement;
};

