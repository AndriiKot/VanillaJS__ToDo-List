/**
 * Creates a list item element (`<li>`) and returns it.
 *
 * @returns {HTMLLIElement} The created list item element.
 */
export const createListItem = () => {
  return document.createElement('li');
};
