{
  "name": "todo-list--vanillajs",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "cross-env NODE_OPTIONS=--experimental-vm-modules jest",
    "test:watch": "cross-env NODE_OPTIONS=--experimental-vm-modules jest --coverage jest --watch",
    "test:coverage": "cross-env NODE_OPTIONS=--experimental-vm-modules jest --coverage",
    "dev": "live-server app",
    "format": "prettier app --write",
    "lint": "eslint app",
    "prepare": "git config core.hooksPath .git-hooks || echo 'Not in a git repo'"
  },
  "devDependencies": {
    "@babel/cli": "^7.27.2",
    "@babel/core": "^7.27.4",
    "@babel/preset-env": "^7.27.2",
    "@eslint/js": "^9.28.0",
    "babel-jest": "^30.0.0",
    "cross-env": "^7.0.3",
    "eslint": "^9.28.0",
    "globals": "^16.2.0",
    "jest": "^30.0.0",
    "jest-environment-jsdom": "^30.0.0",
    "live-server": "^1.2.2",
    "prettier": "^3.5.3"
  }
}
