{
  "name": "todo-list--vanillajs",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "dev": "live-server app",
    "format": "prettier app --write",
    "lint": "eslint app",
    "prepare": "git config core.hooksPath .git-hooks || echo 'Not in a git repo'"
  },
  "devDependencies": {
    "@eslint/js": "^9.28.0",
    "eslint": "^9.28.0",
    "globals": "^16.2.0",
    "jest": "^30.0.0",
    "live-server": "^1.2.2",
    "mock-fs": "^5.0.0",
    "prettier": "^3.5.3"
  }
}
